###基础1
``` 
Linux有7个控制窗口， 按Ctrl + Alt + F1-F7切换
1-6为命令控制窗口，7为图像控制窗口

```

###基础2
```
以.开头的文件都是隐藏文件
```

###基础3
```
Linux的命令区分大小写
```

###基础4
```
Linux的命令帮助文档路径 /user/share/doc
```

###基础5
```
正确的关机方法：
    数据同步写入磁盘：sync
    惯用的关机命令：shutdown
    重启、关机：reboot,halt,poweroff
```

###基础6
```
运行级别 7 个 ：rc0.d-rc6.d
在目录 /etc/rc.d下
每个级别文件夹里面有对应的服务（都是快捷方式）
级别文件夹里面 凡是以K开头的都是不允许的，凡是S开头的都是运行的。
```

###基础7
```
忘记root密码
   重启系统
   在等待界面按任意键 然后 按e 选择第2项kernel/vmlinuz.....
   在按e 进入编辑模式 输入空格和1或者s（进入1级别） 回车 再次回到选择界面 按b重新引导系统
   当系统运行到可以进行命令操作的时候 输入 passwd 重新设置密码
   重设置完密码后， 输入 exit退出用户，重新启动
```

###基础7
```
用户的配置信息都在 /etc/passwd 文件里面
用户的密码信息都在 /etc/shadow 文件里面
```

###文件结构
``` 
ls -l 查看当前目录的时候
文件开头          -：表示普通文件 
                  d：表示目录 
                  b：设备文件（块设备，可以存储东西的设备，例如U盘等）
                  c：设配文件（字符文件，例如鼠标等）
                  l：软连接（快捷方式）

后面的9个字符     ---       ---       ---
                 用户       组         其它
                   rwx       rwx       rwx
                 r 4 读     w 2 写    x 1 执行
```

###文件后缀
```
在Linux系统里面，文件的后缀是不重要的，除了一些特别的文件
```

###目录结构
```
bin        : 存放2进制文件，例如命令
boot       : 系统启动文件和内核
dev        : 设备相关，U盘等
etc        : 配置文件
home       : 各个用户的目录（除了root）
lib        : 链接库文件
lost+found : 
media      : 挂载点，U盘等
mnt        ：挂载点
net        : 
opt        : 源码软件安装路径
proc       : 内核参数（没有文件，虚文件）
root       : root用户的家目录
sbin       : 2进制文件，系统管理员的命令存放
tmp        : 临时文件
user       : 软件的默认安装路径
var        : 日志，缓存的

```

###权限
```
默认的文件创建权限为 				666
默认的文件夹创建权限为 			777
但因为有权限过滤002，所以实际为		664
								775
r		可读
w		可写
x		可执行
s		(SUID当用于文件的时候)当普通用户执行的时候，也将会拥有所有者的权限，即所有者在执行的时候，如果要修改某个文件，那么普通用户在执行的时候也将会修改该文件。权限一样。(SGID当用于文件夹的时候)无论谁在该文件夹里面创建的东西，都会属性该文件夹所属的族，而不是该文件夹所有者的族。
t		(SBIT只能用于other)该文件除了所有者和root之外，其余的人都不能够删除.

权限第一位数字	0
				1		o+t
				2		g+s
				4		u+s
```

###中文乱码
```
vi/etc/sysconfig/i18n将内容改为LANG="zh_CN.GB18030"LANGUAGE="zh_CN.GB18030:zh_CN.GB2312:zh_CN"SUPPORTED="zh_CN.GB18030:zh_CN:zh:en_US.UTF-8:en_US:en"SYSFONT="lat0-sun16"重新登录。
```
